void setup(){
  int[] tab=new int[10];
  initBoard(tab,3);
affiTab(tab);
}

void affiTab(int[]tab){
println (tab);
}

void initBoard (int[] tab, int nbMines){
    
  //for (int i =0; i<tab.length; i++){    
    
    /// Tant que j'ai des mines a poser
    while(nbMines!=0) {
     // Choisir case aleatoire
     int value=(int)random(tab.length);
     // Si case contient 0 alors
     if(tab[value]==0){
       //Poser mine
       tab[value]=99;
       nbMines--;
     }
    }
    
